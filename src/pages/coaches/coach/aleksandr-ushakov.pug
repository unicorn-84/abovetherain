extends ../../../templates/layout
append variables
  - const { options, pages, primaryNavigation } = require('../../../data');
//-HEAD
append meta
  //Page meta tags
  //FIXME: 'Проверять title'
  title= pages.aleksandrUshakov.title
  //FIXME: 'Проверять description'
  meta(name='description', content= pages.aleksandrUshakov.description)
append icons
  //Page icons
append styles
  //Page styles
  each cssFile in htmlWebpackPlugin.files.css
    if cssFile.search(/inline/i) === -1 && cssFile.search(/coach\./i) !== -1
      link(rel='preload', href= cssFile.substr(htmlWebpackPlugin.files.publicPath.length), as='style', onload='this.onload=null\;this.rel=\'stylesheet\'')
      noscript
        link(rel='stylesheet', href= cssFile.substr(htmlWebpackPlugin.files.publicPath.length))
  //Inline styles
  each cssFile in htmlWebpackPlugin.files.css
    if cssFile.search(/inline~coach\./i) !== -1
      style= compilation.assets[cssFile.substr(htmlWebpackPlugin.files.publicPath.length)].source()
//-BODY
append header
  //Header
  +header(primaryNavigation)
append content
  +pageTitle(pages.aleksandrUshakov)
  section#content
    //-Content
    div(class='content-wrap')
      //-Container
      div(class='container')
        div(class='row justify-content-center')
          div(class='col-12 col-lg-8')
            div(class='card border-0')
              div(class='card-body p-sm-4')
                h2(class='card-title mb-3')
                  |Александр Ушаков
                span(class='card-subtitle mb-4')
                  |Лицензированный инструктор программ <strong>Zumba (R) Fitness</strong> и <strong>Strong by Zumba (R)</strong>
                p(class='card-text lead mb-4')
                  |В составе хореографического танцевального ансамбля "Nord Star" многократный призер и лауреат различных степеней международного конкурса хореографического искусства "Волна успеха"; бронзовый призер всероссийского конкурса талантов "Звезда Удачи".
                p(class='card-text lead mb-4')
                  |Участник и презентр городских спортивных мероприятий (мастер-классов, флеш-мобов, конвенций).
                blockquote(class='blockquote mb-0')
                  p(class='mb-0')
                    |В работе главное - взаимосвязь между инструктором и учеником.
                  footer(class='blockquote-footer')
                    cite(title='Александр Ушаков') Александр Ушаков
append scripts
  //Page scripts
  each jsFile in htmlWebpackPlugin.files.js
    if jsFile.search(/inline/i) === -1 && jsFile.search(/coach\./i) !== -1
      script(type='text/javascript', src= jsFile.substr(htmlWebpackPlugin.files.publicPath.length))
  //Inline scripts
  each jsFile in htmlWebpackPlugin.files.js
    if jsFile.search(/inline~coach\./i) !== -1
      script(type='text/javascript') !{compilation.assets[jsFile.substr(htmlWebpackPlugin.files.publicPath.length)].source()}
