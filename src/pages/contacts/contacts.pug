extends ../../templates/layout
append variables
  -const { common, options, pages, primaryNavigation, contacts } = require('../../data');
//-HEAD
append meta
  //Page meta tags
  title= `${common.name} | ${pages.contacts.title}`
  if pages.contacts.description
    meta(name='description', content= pages.contacts.description)
  else
    meta(name='description', content= common.description)
  if pages.contacts.keys
    meta(name='keywords', content= pages.contacts.keys)
append icons
  //Page icons
append styles
  //Page styles
//-BODY
append header
  +header(primaryNavigation.items)
append content
  +pageTitle(pages.contacts)
  section#content
    .content-wrap
      .container.clearfix
        .row
          .col-lg-12
            h1= mark
            .contact-content
              .contact-address
                i.icon-map-marker
                | !{contacts.address}
              .contact-phone
                i.icon-phone3
                //-TODO изменить телефон
                a(href=`tel:${contacts.phone}`)= contacts.phone
              .contact-email
                i.icon-envelope-alt
                //-TODO data-ajax contact form
                a(href="#")= contacts.email
          .col-lg-12
            #yandex-map
append scripts
  //Page scripts
  script(src="https://api-maps.yandex.ru/2.1/?lang=ru_RU" type="text/javascript")

