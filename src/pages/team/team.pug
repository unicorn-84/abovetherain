extends ../../templates/layout
append variables
  -const { options, pages, primaryNavigation, team } = require('../../data');
  -const teamPhoto = require('../../images/team/thumbnails/team.jpg');
  -const bigTeamPhoto = require('../../images/team/team.jpg');
//-HEAD
append meta
  //Page meta tags
  title= `${options.siteName} | ${pages.team.title}`
  meta(name="description" content= pages.description)
append icons
  //Page icons
append styles
  //Page styles
//-BODY
append header
  +header(primaryNavigation.items)
append content
  +pageTitle(pages.team)
  section#content
    .content-wrap
      .container
        .row
          .col
            .team-wrapper
              //-TODO Слайдер для 'Мы'
              .team-photo
                picture
                  source(media="(min-width: 768px)" srcset= bigTeamPhoto)
                  img(src= teamPhoto alt="Над Дождём")
              .team-content
                .team-title !{team.title}
                .team-description !{team.description}
append scripts
