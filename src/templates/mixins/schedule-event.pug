mixin schedule-event(rowspan, directionNumber, direction, coachNumber, coach, timeStart, timeEnd)
  td.schedule-event(rowspan= rowspan data-direction= directionNumber data-coach= coachNumber)
    if direction
      .schedule-title
        a(href='services.html') !{direction}
      .schedule-interval
        span= `${timeStart}-${timeEnd}`
    if coach
      .schedule-coach
        a(href='coaches.html')= coach
