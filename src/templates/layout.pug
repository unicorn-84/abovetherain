//-Common variables
block variables
//-Markup
doctype html
html(lang='RU-ru', dir='ltr', prefix='og: //ogp.me/ns#')
  head
    block meta
      include ./blocks/meta
    block icons
      include ./blocks/icons
    block styles
      include ./blocks/styles
  body.stretched.no-transition
    if htmlWebpackPlugin.options.server !== 'dev'
      //Yandex.Metrika counter
      script.
        (function (d, w, c) {
          (w[c] = w[c] || []).push(function () {
            try {
              w.yaCounter51313063 = new Ya.Metrika2({
                id: 51313063,
                clickmap: true,
                trackLinks: true,
                accurateTrackBounce: true,
                webvisor: true
              });
            } catch (e) { }
          });

          var n = d.getElementsByTagName("script")[0],
            s = d.createElement("script"),
            f = function () { n.parentNode.insertBefore(s, n); };
          s.type = "text/javascript";
          s.async = true;
          s.src = "https://mc.yandex.ru/metrika/tag.js";

          if (w.opera == "[object Opera]") {
            d.addEventListener("DOMContentLoaded", f, false);
          } else { f(); }
        })(document, window, "yandex_metrika_callbacks2");
      noscript
        div
          img(src='https://mc.yandex.ru/watch/51313063', style='position:absolute; left:-9999px;', alt='')
      ///Yandex.Metrika counter
    //Page content
    #wrapper
      block header
        include ./mixins/header
      block content
        include ./mixins/page-title
      block footer
        include ./blocks/footer
    block scripts
      include ./blocks/scripts
